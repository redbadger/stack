domains:
  - name: dev
    stacks:
      - name: services
        services:
          - name: visualizer
      - name: app
        services:
          - name: rproxy
            aliases:
              - web
