stacks:
  - name: services
    compose-files:
      - services.yml
    services:
      - name: visualizer
  - name: app
    compose-files:
      - app.yml
    services:
      - name: rproxy
        aliases:
          - web
